# Software Submission - CampusPulse

Use this folder as the software-only submission packet.

## Submit

- Public GitHub repository link after pushing `campuspulse/`.
- Live deployed app link after hosting it on Render, Railway, Fly.io, or another cloud host.
- `Software_CampusPulse_READY_TO_SEND.zip` if the portal accepts zipped project files.

## Local Project

The ready project copy is:

- `campuspulse/`

Run locally:

```bash
npm start
```

Run verification:

```bash
npm test
```

## Notes

- The app is a complete CDI solution for CampusPulse: a student-facing campus issue tracker with reporting, voting, filtering, status updates, analytics, CSV export, and a polished 3D interface.
- The Dockerfile is included in `campuspulse/Dockerfile`.
- The README inside `campuspulse/README.md` contains setup instructions, API endpoints, features, Docker usage, deployment notes, and evaluation mapping.
